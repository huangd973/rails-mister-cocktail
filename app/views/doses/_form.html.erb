<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: Ingredient.all, input_html: { class: 'multiselectuser', multiple: true} %>

  <%= f.submit "add a cocktail", class: "btn btn-primary" %>
<% end %>
